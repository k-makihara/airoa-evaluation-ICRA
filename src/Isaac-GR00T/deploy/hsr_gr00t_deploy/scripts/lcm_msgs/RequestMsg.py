"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

import lcm_msgs

class RequestMsg(object):

    __slots__ = ["head_rgb", "hand_rgb", "joint_state", "instruction"]

    __typenames__ = ["lcm_msgs.CompressedImage", "lcm_msgs.CompressedImage", "lcm_msgs.JointState", "string"]

    __dimensions__ = [None, None, None, None]

    def __init__(self):
        self.head_rgb = lcm_msgs.CompressedImage()
        """ LCM Type: lcm_msgs.CompressedImage """
        self.hand_rgb = lcm_msgs.CompressedImage()
        """ LCM Type: lcm_msgs.CompressedImage """
        self.joint_state = lcm_msgs.JointState()
        """ LCM Type: lcm_msgs.JointState """
        self.instruction = ""
        """ LCM Type: string """

    def encode(self):
        buf = BytesIO()
        buf.write(RequestMsg._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.head_rgb._get_packed_fingerprint() == lcm_msgs.CompressedImage._get_packed_fingerprint()
        self.head_rgb._encode_one(buf)
        assert self.hand_rgb._get_packed_fingerprint() == lcm_msgs.CompressedImage._get_packed_fingerprint()
        self.hand_rgb._encode_one(buf)
        assert self.joint_state._get_packed_fingerprint() == lcm_msgs.JointState._get_packed_fingerprint()
        self.joint_state._encode_one(buf)
        __instruction_encoded = self.instruction.encode('utf-8')
        buf.write(struct.pack('>I', len(__instruction_encoded)+1))
        buf.write(__instruction_encoded)
        buf.write(b"\0")

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != RequestMsg._get_packed_fingerprint():
            raise ValueError("Decode error")
        return RequestMsg._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = RequestMsg()
        self.head_rgb = lcm_msgs.CompressedImage._decode_one(buf)
        self.hand_rgb = lcm_msgs.CompressedImage._decode_one(buf)
        self.joint_state = lcm_msgs.JointState._decode_one(buf)
        __instruction_len = struct.unpack('>I', buf.read(4))[0]
        self.instruction = buf.read(__instruction_len)[:-1].decode('utf-8', 'replace')
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if RequestMsg in parents: return 0
        newparents = parents + [RequestMsg]
        tmphash = (0xb940022b66a523f5+ lcm_msgs.CompressedImage._get_hash_recursive(newparents)+ lcm_msgs.CompressedImage._get_hash_recursive(newparents)+ lcm_msgs.JointState._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if RequestMsg._packed_fingerprint is None:
            RequestMsg._packed_fingerprint = struct.pack(">Q", RequestMsg._get_hash_recursive([]))
        return RequestMsg._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", RequestMsg._get_packed_fingerprint())[0]

