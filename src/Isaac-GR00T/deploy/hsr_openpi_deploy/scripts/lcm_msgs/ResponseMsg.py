"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class ResponseMsg(object):

    __slots__ = ["hz", "num_joints", "joint_names", "rows", "cols", "result"]

    __typenames__ = ["double", "int32_t", "string", "int32_t", "int32_t", "double"]

    __dimensions__ = [None, None, ["num_joints"], None, None, ["rows", "cols"]]

    def __init__(self):
        self.hz = 0.0
        """ LCM Type: double """
        self.num_joints = 0
        """ LCM Type: int32_t """
        self.joint_names = []
        """ LCM Type: string[num_joints] """
        self.rows = 0
        """ LCM Type: int32_t """
        self.cols = 0
        """ LCM Type: int32_t """
        self.result = []
        """ LCM Type: double[rows][cols] """

    def encode(self):
        buf = BytesIO()
        buf.write(ResponseMsg._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">di", self.hz, self.num_joints))
        for i0 in range(self.num_joints):
            __joint_names_encoded = self.joint_names[i0].encode('utf-8')
            buf.write(struct.pack('>I', len(__joint_names_encoded)+1))
            buf.write(__joint_names_encoded)
            buf.write(b"\0")
        buf.write(struct.pack(">ii", self.rows, self.cols))
        for i0 in range(self.rows):
            buf.write(struct.pack('>%dd' % self.cols, *self.result[i0][:self.cols]))

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ResponseMsg._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ResponseMsg._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = ResponseMsg()
        self.hz, self.num_joints = struct.unpack(">di", buf.read(12))
        self.joint_names = []
        for i0 in range(self.num_joints):
            __joint_names_len = struct.unpack('>I', buf.read(4))[0]
            self.joint_names.append(buf.read(__joint_names_len)[:-1].decode('utf-8', 'replace'))
        self.rows, self.cols = struct.unpack(">ii", buf.read(8))
        self.result = []
        for i0 in range(self.rows):
            self.result.append(struct.unpack('>%dd' % self.cols, buf.read(self.cols * 8)))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if ResponseMsg in parents: return 0
        tmphash = (0xf38464883c910550) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if ResponseMsg._packed_fingerprint is None:
            ResponseMsg._packed_fingerprint = struct.pack(">Q", ResponseMsg._get_hash_recursive([]))
        return ResponseMsg._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", ResponseMsg._get_packed_fingerprint())[0]

